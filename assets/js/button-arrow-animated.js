(function () {
  document.addEventListener("DOMContentLoaded", function () {
    // (nessuna logica JS obbligatoria per ora — tutto è gestito da CSS)
    // lasciato vuoto per futuri effetti o ottimizzazioni
  });
})();
